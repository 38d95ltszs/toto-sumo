# トト相撲 データ取得要件（取得先：相撲レファレンス）

フェッチ担当AIへの依頼書。ゲーム「トト相撲」が必要とする本場所データを、相撲レファレンス（Sumo Reference / sumodb 系）から取得し、下記の `data/basho.json` 形式で出力してほしい。現行はスポーツナビから取得しているが、取得先を相撲レファレンスに切り替える。**ゲーム本体（index.html）はこのJSONの形さえ守れば無改修で動く**ため、最終的なアウトプットの構造を厳守すること。

---

## ⚠ 最初に判断が必要な事項（ここが決まらないと全体が動かない）

### 1. 力士名は「日本語の漢字しこ名」で返すこと（最重要）
ゲーム内部は全て漢字（例：`豊昇龍`・`大の里`・`若隆景`）で力士を識別している。一門マップ・推し力士・番付表・称号すべてが漢字名キーに依存する。
相撲レファレンスはローマ字表記（例：`Hoshoryu`・`Onosato`）が主体のため、**そのまま取ると全力士が一致しなくなる**。
- 必須：出力する力士名は**必ず日本語の漢字しこ名**にすること。
- 相撲レファレンスに漢字しこ名フィールドがあればそれを使う。無ければローマ字→漢字の対応表を別途用意する（現役幕内〜十両で数十名程度）。
- 判断がつかない場合は、この点だけ先に相談してほしい。

### 2. 日本語ニュース見出しは対象外
現行はYahoo!ニュースの見出しを `news` に入れているが、相撲レファレンスにはこの情報がない。**news は取得対象から外す**（JSONからは省略可。ゲーム側は無くても動作する）。

### 3. 場所の同定
- 場所は奇数月（1・3・5・7・9・11月）、第2日曜が初日の15日間。
- 出力の `bashoKey` は `"YYYY-MM"` 形式（例：2026年9月場所 → `"2026-09"`）。

---

## 取得してほしいデータ（ユーザー指定の4項目 ＝ 過不足なくこれで足りる）

| 分類 | 内容 | 出力先フィールド |
|---|---|---|
| ① 番付 | その場所の幕内・十両の番付（力士名・番付・東西） | `banzuke` |
| ② 勝敗・③ 取組結果 | 各日の幕内全取組（対戦カード・勝者・決まり手・不戦の有無） | `days` |
| ④ 優勝・三賞 | 幕内最高優勝、殊勲賞・敢闘賞・技能賞 | `awards` |
| （付随）休場 | 各日の休場力士 | `kyujoLog` / `injuredCarry` |
| （付随）来場所番付 | 番付予想モードの答え合わせ用（後述） | `nextBanzuke` |

---

## 出力JSONの完全仕様（`data/basho.json`）

```jsonc
{
  "bashoKey": "2026-09",                 // "YYYY-MM"
  "updatedAt": "2026-09-14T03:00:00Z",   // 取得時刻 ISO8601（任意）
  "source": "sumo-reference",            // 任意

  // ① 番付：その場所の幕内＋十両を全員。番付予想の対象が前頭上位まで及ぶため十両まで取る
  "banzuke": [
    { "name": "豊昇龍", "rank": "Y",  "side": "東" },
    { "name": "大の里", "rank": "Y",  "side": "西" },
    { "name": "霧島",   "rank": "O",  "side": "東" },
    { "name": "熱海富士","rank": "S",  "side": "東" },
    { "name": "藤ノ川", "rank": "M1", "side": "東" },
    { "name": "誰それ", "rank": "J1", "side": "東" }
    // …幕内42名＋十両（少なくとも十両上位数枚まで）
  ],

  // ②③ 取組結果：キーは「日目（1〜15）」の文字列。値はその日の幕内全取組の配列
  "days": {
    "1": [
      { "e": "豊昇龍", "w": "琴櫻",   "k": "寄り切り", "win": "e", "fusen": false },
      { "e": "大の里", "w": "霧島",   "k": "押し出し", "win": "w", "fusen": false },
      { "e": "若隆景", "w": "安青錦", "k": null,       "win": null, "fusen": false }  // 未確定の取組
    ],
    "2": [ /* … */ ]
    // 進行中の場所では「確定した日まで＋翌日の取組カード（結果はnull）」でよい
  },

  // ④ 優勝・三賞：優勝力士が未発表なら awards は null（千秋楽以降に取得）
  "awards": {
    "yusho":  { "name": "豊昇龍", "rank": "横綱", "record": "14勝1敗", "heya": "立浪" },
    "shukun": [ { "name": "…", "rank": "…", "record": "…", "heya": "…" } ],  // 該当なしは []
    "kanto":  [ /* 敢闘賞・複数可 */ ],
    "gino":   [ /* 技能賞・複数可 */ ]
  },

  // 付随：休場。キー=日目、値=その日休場していた力士名の配列
  "kyujoLog": { "3": ["宇良"], "7": ["宇良", "朝乃山"] },

  // 付随：前場所5日以上休場した力士（今場所「怪我明け」補正の対象）。分からなければ []
  "injuredCarry": ["朝乃山"],

  // 付随：来場所の新番付（番付予想モードの答え合わせ用・後述）
  "nextBanzuke": {
    "bashoKey": "2026-11",
    "fetchedAt": "2026-10-27T00:00:00Z",
    "list": [
      { "name": "豊昇龍", "rank": "Y", "side": "東" }
      // …少なくとも横綱〜前頭5枚目まで（東西両側）
    ]
  }
}
```

---

## フィールドごとの厳密ルール

### `banzuke`（番付）
- 各要素 `{ name, rank, side }`。
- `rank` は**正規化コード**で返す：
  - 横綱=`Y` / 大関=`O` / 関脇=`S` / 小結=`K`
  - 前頭n枚目=`M` + 数字（前頭筆頭=`M1`、前頭2枚目=`M2` …）
  - 十両n枚目=`J` + 数字（十両筆頭=`J1` …）
- `side` は `"東"` または `"西"`（`"E"`/`"W"` でも可。ゲーム側は両方解釈する）。
- 幕内全員（通常42名）は必須。番付予想モードのため**十両も上位数枚まで**含めると安全。

### `days`（取組結果）
- キーは日目の文字列 `"1"`〜`"15"`。
- 各取組 `{ e, w, k, win, fusen }`：
  - `e` = 東方力士名、`w` = 西方力士名（**漢字しこ名**）。
  - `k` = 決まり手（**日本語**。例：`寄り切り`・`押し出し`・`上手投げ`）。未確定なら `null`。
  - `win` = 勝者。東の勝ち=`"e"`、西の勝ち=`"w"`、未確定=`null`。
  - `fusen` = 不戦勝／不戦敗なら `true`（休場による不成立）。それ以外 `false`。
- **並び順**：現行ゲームは「配列の末尾が結びの一番（進行の逆順で掲載）」という前提で動いている。相撲レファレンスの掲載順がこれと違う場合は、**取組を実際の進行順（序盤→結び）に並べ替えたうえで、どちら向きに並んでいるかを明記**してほしい（ゲーム側の1行フラグ `realOrder` で吸収できるため、向きさえ分かればよい）。
- 進行中の場所は「確定済みの日まで＋（あれば）翌日の取組カード（`win:null`）」で足りる。過去日の確定結果は上書きしないこと。

### `awards`（優勝・三賞）
- `yusho` が取れなければ **`awards` 全体を `null`** にする（＝未発表とみなす。ゲームはこれで場所を締めずに待つ）。
- `shukun`／`kanto`／`gino` は各賞とも複数受賞があり得るので**配列**。該当なしは空配列 `[]`。
- 各受賞者 `{ name, rank, record, heya }`。`rank`・`record`・`heya` は分かる範囲で（`null` 可）。優勝決定戦（plassoff）はゲーム側で成績から推定するので不要。

### `kyujoLog` / `injuredCarry`（休場）
- `kyujoLog`：キー=日目、値=その日休場していた力士名の配列。取れなければ省略可（`{}` 相当）。
- `injuredCarry`：前場所に**5日以上**休場した力士名の配列（今場所を通してオッズ補正）。判定が難しければ空配列でよい。

### `nextBanzuke`（来場所番付・番付予想モード用）
- **取得タイミング**：番付は本場所の**2週間前の月曜**に正式発表される。オフシーズン期間中、次の場所ページに新番付が掲載されたら取得する。
- `list` は `banzuke` と同じ `{ name, rank, side }` 形式。**最低でも横綱〜前頭5枚目**（東西）まであれば答え合わせが成立する（幕内全員あればなお良い）。
- 発表前で取得できなければ `nextBanzuke` は省略（後日リトライ）。

---

## 運用ルール（現行スクレイパーの作法を踏襲）

- **頻度**：1日3回程度の低頻度アクセス。行儀よく（リクエスト間に1〜2秒の間隔）。
- **場所期間外**：本場所をやっていない期間は、上記 `nextBanzuke`（番付発表後）以外は取得不要。
- **マージ**：既存 `data/basho.json` があれば、過去日の確定結果は保持し、新しく確定した分だけ上書き更新する（確定済みを未確定で上書きしない）。
- **優勝・三賞は一度取れたら保持**：千秋楽後に取得できたら以降は再取得しない。
- **出力先**：`data/basho.json`（UTF-8・JSON）。1ファイルに集約。

---

## ゲームが「使わない」データ（取らなくてよい）
- 個々の力士の身長体重・生年月日・通算成績・履歴
- 序ノ口〜幕下の取組（幕内のみ。番付は十両まで）
- 写真・画像・英語の解説記事
- 日本語ニュース見出し（前述のとおり対象外）

---

## まとめ（ユーザーの当初の想定への回答）
当初挙げてもらった「番付・勝敗・取組結果・優勝と三賞」の4点で**過不足なく足りている**。強いて足すなら、
- 休場情報（`kyujoLog` / `injuredCarry`）… 不戦勝の返還処理と怪我明けオッズ補正に使う。無くても動くが、あると実データの再現度が上がる。
- 来場所番付（`nextBanzuke`）… 番付予想モードの答え合わせに使う。オフシーズンにのみ必要。

最重要は「**力士名を漢字しこ名で返すこと**」。ここだけは取得先を相撲レファレンスに変える最大の障壁なので、対応方針（漢字フィールドの有無／対応表の要否）を最初に確定させてほしい。
