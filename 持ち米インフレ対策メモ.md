# 持ち米インフレ／iPhoneクラッシュ 対策メモ

令和8年七月場所の終了直後に発生した「iPhoneで米取表タブがクラッシュ」「Supabase同期が400エラー」の原因と対策の記録。

## 何が起きたか

- 症状1：iPhone（Safari／Chrome＝どちらもiOSではWebKit）で **米取表タブを開くとクラッシュ**。PCブラウザでは問題なし。
- 症状2：PCブラウザのコンソールに同期エラー。
  ```
  POST .../rest/v1/players 400 (Bad Request)
  同期失敗: value "10612510938" is out of range for type integer
  ```
- 持ち米（points）が **約106億** という異常値になっていた（`40000 × 2^18 ≈ 104.9億`）。

## 根本原因

優勝予想・三賞予想の倍付け（＝千秋楽終了時の持ち米を丸ごと倍化する演出）を行う `processFinish()` が、**再実行のたびに倍付けを再適用**していた。テスト運用や状態復元の過程で処理が繰り返され、持ち米が指数的に膨張した。

この1つの異常値が、2つの症状を同時に引き起こしていた。

1. **iPhoneクラッシュ**：収支グラフ `drawChart()` の目盛り線ループ `for (v=0; v<=maxV; v+=10000)` が、持ち米106億だと **100万回以上** 反復。iOSがCPU/メモリ負荷でタブを強制終了していた（PCは遅いが完走するので再現しなかった）。
2. **同期400エラー**：`players.points` 列は Postgres の `integer` 型（上限 約21.4億 = 2³¹−1）。106億は範囲外で `upsert` が拒否されていた。

## 実施した修正（index.html）

| # | 対策 | 内容 |
|---|---|---|
| 1 | 倍付けの冪等化 | 優勝・三賞の精算に `settled` フラグを付与。一度適用したら二度と倍付けしない。再実行しても持ち米は据え置き（検証済み：40000→138240→138240→…） |
| 2 | グラフループの上限化 | `drawChart()` の目盛り線を最大約8本に固定（`gridStep = max(10000, ceil(maxV/8/10000)*10000)`）。持ち米が異常値でも一瞬で描画完了（106億でも1ms） |
| 3 | 同期値のクランプ | `pushProfile()` で points 等を integer 安全域（±20億）に丸めてから送信。異常値でも400で失敗し続けない（表示用の `G.points` は変更しない） |

あわせて、別端末ログイン時の欠損フィールド補完（`hydrateG`）、`fmt()` のnull安全化、描画処理の `try/catch` 安全網も追加済み。

## 既存の異常口座（106億）について

- 水曜10:00の **トト番付編成会議で自動リセット**される（編成会議後は「基本15,000＋番付ボーナス」で再スタートするため）。
- 待たずに直すなら、その端末で米取表の「⚠ 全データをリセット」で作り直す。

## integer上限（約21億）を bigint に上げるべきか → 上げない

「上限を突破できないか？」への結論。

- **技術的には可能**：列を `bigint`（上限 約922京）に変えれば実質青天井。
  ```sql
  alter table public.players
    alter column points       type bigint,
    alter column final_points type bigint;
  -- 過去アーカイブも上げる場合
  alter table public.basho_results
    alter column final_points type bigint;
  ```
- **しかし不要・非推奨**。
  - 持ち米は毎場所リセット（初期1.5万〜4.5万）。1場所で正当に増える上限は、優勝予想×15＋三賞の重ね掛けを全部当てても **せいぜい数百万合**。21億はその400倍以上の余裕があり、正常な遊びでは絶対に届かない。
  - 106億は完全にバグ由来で、それは冪等化（対策1）で塞いだ。
  - 21億のクランプは「ここに達したら異常＝バグ」を早期検知する **安全弁** として有用。bigintで上限を外すと、同種バグが出たとき桁が青天井に膨らんでから気づくことになる。
  - もし bigint にするなら、クライアントの `clampInt`（現在20億）も同時に引き上げないと、結局20億で頭打ちになる。

**方針：`integer` ＋ クランプ（20億） ＋ 冪等化 のまま運用する。**

## 教訓

- 「倍付け」系の処理は必ず一度きり（冪等）に。再実行や再精算で二重適用されない設計にする。
- 数値を描画やループに使う箇所は、値が異常に大きくても破綻しない上限を設ける（特にモバイルは負荷に弱い）。
- DBへ書く数値はカラム型の範囲に収まるようクライアント側でもクランプしておく。
