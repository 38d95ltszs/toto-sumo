# サーバー精算（強制精算）設定手順

千秋楽後にアプリを開いていないプレイヤーの分も含め、**サーバー側で全員の最終持ち米を確定**する仕組み。これにより水曜10:00の番付編成会議で、トト番付（相対順位）が正しく決まる。

## 仕組みの要点

- Supabaseの **Edge Function `settle-basho`** が、公開中の `data/basho.json`（15日分の結果＋優勝三賞）と全プレイヤーの `player_saves` を読み、各人の未精算ベットを精算して `players.final_points` を更新する。
- 精算ロジックはクライアント（index.html）と同一。**node検証で「サーバー精算＝クライアント精算」の完全一致を確認済み**（一度も開かなかった人・途中まで開いた人のどちらも一致）。
- **GitHub Actions の cron** が1日1回このFunctionを呼ぶ。`awards` 未発表（本場所中や平常時）は自動でスキップするので、毎日走っても安全。

> **SQLは不要**。既存の `players` / `player_saves` テーブルの列だけで動く（`final_points`・`hits`・`bets` を更新するだけ）。

---

## セットアップ（3ステップ）

### STEP 1: Edge Function を配置・デプロイ

1. リポジトリ（またはSupabaseプロジェクト）に、同梱の `settle-basho.index.ts` を次のパスで置く：
   ```
   supabase/functions/settle-basho/index.ts
   ```
2. 秘密情報を設定（`SETTLE_SECRET` は好きな長い文字列。cronと共有する合言葉）：
   ```bash
   supabase secrets set SETTLE_SECRET="長くてランダムな文字列をここに"
   # data/basho.json の公開URLがデフォルトと違う場合のみ設定（既定は本番URL）
   supabase secrets set BASHO_DATA_URL="https://<ユーザー名>.github.io/toto-sumo/data/basho.json"
   ```
   > `SUPABASE_URL` と `SUPABASE_SERVICE_ROLE_KEY` はSupabaseが自動で注入するので設定不要。
3. デプロイ（**`--no-verify-jwt` を必ず付ける**。認証は下記の合言葉ヘッダで行うため）：
   ```bash
   supabase functions deploy settle-basho --no-verify-jwt
   ```

### STEP 2: cron（GitHub Actions）を配置

1. 同梱の `settle-basho.yml` を `.github/workflows/settle-basho.yml` として置く。
2. ファイル内の `FUNCTION_URL` を自分のプロジェクトに合わせる：
   ```
   https://<プロジェクトRef>.supabase.co/functions/v1/settle-basho
   ```
   （既定は現行プロジェクトのURL。変わっていなければそのままでOK）
3. GitHubリポジトリの **Settings → Secrets and variables → Actions** で、`SETTLE_SECRET` を **STEP1と同じ値**で登録。

### STEP 3: 動作確認

- GitHubの「Actions」→「settle basho (server-side)」→「Run workflow」で手動実行。
- レスポンス例：
  - 本場所中／優勝三賞未登録：`{"skipped":"awards未発表のため精算しません", ...}`
  - 精算実行時：`{"ok":true,"bashoKey":"2026-07","players":N,"updated":N,"results":[...]}`

---

## 運用フロー（毎場所）

1. 千秋楽の結果と優勝・三賞を登録ツールで `data/basho.json` に反映・デプロイ。
2. 以降、cronが毎朝サーバー精算を実行 → 全員の `final_points` が確定していく。
3. 水曜10:00の番付編成会議：各プレイヤーの端末が、DB上の正しい `final_points`（開いていない人の分も含む）で相対順位を計算 → トト番付が正しく確定。

> cronは1日1回（JST 06:00）。プレイヤーは千秋楽〜水曜の間もオフシーズン中に随時同期するので、毎日走らせることで最新状態に追従する。

---

## セキュリティ

- `SETTLE_SECRET` は、Edge Function（Supabase Secrets）とGitHub Actions（repo Secret）の**両方に同じ値**を置く。これが一致しないと関数は401を返す（部外者が叩けない）。
- `service_role` キーは **Supabaseが関数内に自動注入**するもので、リポジトリやクライアントには絶対に置かない。
- Functionは `players.final_points`/`hits`/`bets` のみ更新し、`points`（表示用の現在値）や `player_saves` には触れない。プレイヤーが後で開けば、クライアント側の精算が同じ値を書き込むので不整合は起きない。

---

## ルール変更時の注意

精算の倍率やボーナス（オッズ体系・優勝/三賞の倍率など）を変えたら、**index.html と settle-basho.index.ts の両方**を直すこと。両者が一致しているかは、`settlePlayer` をクライアントの `processFinish` と突き合わせるnode検証で確認できる（本対応時に使用したテスト方式）。
